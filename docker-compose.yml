services:
  webscraper-app:
    build: .
    container_name: webscraper-saas
    restart: unless-stopped
    environment:
      - NODE_ENV=production
      - PORT=5000
      - JWT_SECRET=wScrp_8k3Fz7YmNx2Qv9RtBp4LjWd6Hs
    volumes:
      - ./data:/app/data
    networks:
      - npm-network

networks:
  npm-network:
    external: true
    name: nginx-proxy-manager_default
